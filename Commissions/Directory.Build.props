<?xml version="1.0" encoding="utf-8"?>
<Project>
  <!-- Commission plugins use all repository defaults and only change their mod folder. -->
  <PropertyGroup>
    <PluginModName>Commissions</PluginModName>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)..\Directory.Build.props" />
</Project>
